run:
  timeout: 3m

linters:
  enable:
    - goimports
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - revive

issues:
  exclude-use-default: false

linter-settings:
  goimports:
    local-prefixes: github.com/lucrumx/bot